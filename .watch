#!/usr/bin/env bash
DELTARUNEPID=""
while true; do
    sleep 3;
    DELTARUNEPID=$(pidof deltarune)
    	if [ "$DELTARUNEPID" == "" ]; then
    	echo "Thanks for playing, hope you had fun :D"
		kill -9 $1
		kill -9 $2
		kill -9 $$
	fi
done
